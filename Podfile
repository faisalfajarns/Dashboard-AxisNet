# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CloneAxisNet' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CloneAxisNet
     pod 'RxSwift', '6.5.0'
     pod 'RxCocoa', '6.5.0'
     pod 'Firebase/Analytics'
     pod 'Firebase/Core'
     pod 'Firebase/RemoteConfig'

  target 'CloneAxisNetTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CloneAxisNetUITests' do
    # Pods for testing
  end

end
